#include<stdio.h>//Table of given n value number:
#include<conio.h>
void main()
/*
{
    int n,org;
    printf ("Enter a number:");
    scanf ("%d",&n);
    org=n;
    label://label1:orlabel2
        printf ("%d\n",n);
        n=n+org;
    if(n<=org*10)
    goto label;//label1;orlabel2;
    getch();
}
*/

/*
{
    int n,org,i=1;//i=0
    printf ("Enter a number:");
    scanf ("%d",&n);
    org=n;
    label:
        printf ("%d\n",n);
        n=n+org;
        i++;
    if(i!=11)//Or if(i!=10)
    goto label;
    getch();
}
*/

/*
{
    int n,i,a;
    printf ("Enter a number:");
    scanf ("%d",&n);
    for (i=1;i<=10;i++)
    {
        a=n*i;
        printf ("%d\n",a);
    }
}
*/

/*
{
    int n,i,org;
    printf ("Enter any number of which table u need:");
    scanf ("%d",&n);
    org=n;
    for (i=2;i<=11;i++)
    {
        printf ("%d\n",n);
        n=org*i;//when i==1 it becomes 44 when it goes to loop i become i++ or 12 so come out.
    }
getch();
}
*/


